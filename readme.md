A command-line conversational AI agent powered by LangChain and Perplexity Pro, featuring:

Real-time web search
Persistent memory across sessions (stored as JSON)
Clean, styled terminal output with [Rich](https://github.com/Textualize/rich)
Session commands for history, clearing, and switch
Secure API key loading with environment variables